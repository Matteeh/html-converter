{
  "name": "html-pdf-converter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "export NODE_ENV=production node && -r dotenv/config ./dist/src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "nodemon -e ts,json -w src -x 'npm run compile:run'",
    "compile:run": "npm run clean && tsc && npm run run",
    "run": "export NODE_ENV=development && node -r dotenv/config ./dist/src/server.js",
    "build": "npm run clean && tsc",
    "clean": "rimraf ./dist"
  },
  "author": "Mathias Rahikainen",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/node": "^10.11.4",
    "nodemon": "^1.18.4",
    "rimraf": "^2.6.2",
    "typescript": "^3.1.1"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "phantom-html-to-pdf": "^0.5.6"
  }
}
